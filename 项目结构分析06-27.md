# RAG-QA 项目交接文档（资深 Java 架构师逆向分析）

> 项目路径：`/Users/yh/workbench/IdeaProject/RAG-demo`
> 后端模块：`rag-qa-backend/`（Spring Boot 3.3 + Java 17）
> 前端模块：`rag-qa-frontend/`（Vue 3 + Vite，本次未深入）
> 当前分支：`dev`，最近 5 个提交全部围绕 `OcrService` 的 Tess4J / PDFBox / ImageIO 兼容性修复（已落地的技术债修复）

---

## 一、项目概览

### 1.1 业务定位

**RAG-QA 智能问答系统** —— 基于 RAG（Retrieval-Augmented Generation）的私有知识库问答系统。核心能力：

- **多知识库隔离**：用户创建若干知识库，每个知识库独立管理文档与问答上下文
- **多格式文档解析**：支持 PDF（文字层 + 扫描版 OCR）、DOCX（含表格抽取为 Markdown）、XLSX、PPTX、TXT
- **混合检索**：向量检索（Chroma）+ BM25 关键词检索 + RRF 融合排序（已实现但**当前未在主链路生效**）
- **问答双模式**：普通一次性返回 / SSE 流式打字机效果
- **JWT 无状态认证**：Spring Security + jjwt

### 1.2 技术栈全景

| 维度 | 选型 | 关键版本/文件 |
|---|---|---|
| JDK | Java 17 | `pom.xml:21` |
| Web 框架 | Spring Boot 3.3.0 | `pom.xml:9-12` |
| AI 框架 | Spring AI 1.1.3（BOM 引入） | `pom.xml:22,28-35` |
| LLM | Spring AI MiniMax Starter（兼容 OpenAI 协议，可对接硅基流动/MiniMax） | `pom.xml:46-50` |
| Embedding | **Ollama 本地** `qwen3-embedding:4b`（4096 维） | `application.properties:28-29` |
| 向量库 | Chroma v2 API（自实现 REST 客户端，绕开 Spring AI 自动 embedding） | `ChromaService.java:77-108` |
| ORM | Spring Data JPA + Hibernate | `pom.xml:71-75` |
| 数据库 | MySQL 8（驱动 `com.mysql:mysql-connector-j`） | `pom.xml:78-82` |
| 迁移 | Flyway（`flyway-core` + `flyway-mysql`） | `pom.xml:197-204`、`db/migration/V1__init_schema.sql` |
| 认证 | Spring Security + JJWT 0.12.3 | `pom.xml:164-187` |
| API 文档 | springdoc-openapi 2.5.0（Swagger UI） | `pom.xml:189-194` |
| 文档解析 | Apache Tika 2.9.1（core + parsers-standard-package） | `pom.xml:92-103` |
| OCR | Tess4J 5.16.0（JNI 封装） + Apache PDFBox 2.0.31 | `pom.xml:106-127` |
| Office 解析 | Apache POI 5.2.5（poi-ooxml） | `pom.xml:129-135` |
| 配置加载 | dotenv-java 3.0.0（启动时读 `.env` 注入系统属性） | `RagQaApplication.java:32-40` |
| 测试 | JUnit 5（starter-test）+ H2 内存库 + JaCoCo + Pitest | `pom.xml:150-162,222-249` |
| 反应式 | Reactor（仅用于流式问答返回 `Flux<String>`） | `ChatController.java:15` |

### 1.3 模块划分

仓库为**单体 monorepo**（非多 Maven 模块），结构如下：

```
RAG-demo/
├── rag-qa-backend/          # Spring Boot 后端（独立 Maven 项目）
│   ├── src/main/java/com/ragqa/
│   │   ├── RagQaApplication.java        # 启动类
│   │   ├── config/                       # 安全/CORS/OpenAPI/Chroma/全局异常
│   │   ├── controller/                   # 5 个 REST 控制器
│   │   ├── service/                      # 11 个业务服务
│   │   ├── repository/                   # 5 个 Spring Data JPA 仓库
│   │   ├── model/                        # 5 个 JPA 实体
│   │   └── dto/                          # 7 个 DTO
│   ├── src/main/resources/
│   │   ├── application.properties        # 主配置（所有环境）
│   │   └── db/migration/V1__init_schema.sql
│   └── src/test/                         # 7 个测试类，27 个 @Test 方法
├── rag-qa-frontend/         # Vue 3 前端（Vite，package.json，未在本次范围内）
├── chroma/                  # Chroma 持久化目录（docker 卷挂载）
├── uploads/                  # 用户上传文件落地目录
├── docs/                    # SRS/UCD/Design/ST 报告
├── docker-compose.yml       # MySQL + Chroma + Backend + Frontend 一键编排
├── .env / .env.example      # 环境变量模板
└── feature-list.json        # 16 个特性的验收清单（全部 passing）
```

---

## 二、架构与分层

### 2.1 整体架构图

```mermaid
flowchart LR
    Browser[Vue3 Frontend :5173] -->|Bearer JWT| Nginx[Nginx :80 / docker]

    subgraph SpringBoot[Spring Boot 3.3 :8080]
        direction TB
        SF[SecurityFilterChain<br/>+ JwtAuthenticationFilter]
        GC[GlobalExceptionHandler]
        AC[AuthController]
        CC[ChatController / ChatHistoryController]
        DC[DocumentController]
        KC[KnowledgeBaseController]

        US[UserService / JwtService]
        CS[ChatService]
        RS[RagService]
        DBS[DocumentService]
        DPS[DocumentProcessService<br/>@Async]
        KBS[KnowledgeBaseService]

        ES[EmbeddingService<br/>→ Ollama REST]
        CHS[ChromaService<br/>→ Chroma v2 REST]
        TS[TextSplitter]
        OCR[OcrService<br/>Tesseract JNI]
        TX[TableExtractorService<br/>Apache POI]
        HS[HybridSearchService]
        BM[BM25SearchService]
        RR[RerankService]

        Repo[(JPA Repositories)]
        Mysql[(MySQL 8)]
    end

    Nginx --> SF
    SF --> GC
    SF --> AC & CC & DC & KC
    AC --> US
    CC --> CS --> RS
    CC --> CS
    DC --> DBS --> DPS
    DC --> KBS

    DPS --> TS & ES & CHS & BM & OCR & TX
    RS --> HS --> CHS & BM
    RS --> ES
    RS --> RR
    CS --> ChatAI[Spring AI ChatClient<br/>→ OpenAI 兼容 LLM]

    US --> Repo --> Mysql
    KBS --> Repo
    DBS --> Repo
    RS --> Repo
    HS --> ES
```

### 2.2 包结构分层与职责

| 层 | 包 | 职责 | 备注 |
|---|---|---|---|
| 入口 | `com.ragqa` | Spring Boot 启动 | 启动时 `Dotenv` → `System.setProperty` → Spring `${VAR}` 读取 |
| 控制器 | `com.ragqa.controller` | HTTP 端点 | 5 个控制器，**ChatHistoryController 直接使用 Repository，跳过 Service**（见风险） |
| 服务 | `com.ragqa.service` | 业务编排 | 11 个服务，含 RAG 三阶段（切分/Embedding/检索） |
| 配置 | `com.ragqa.config` | 安全、CORS、OpenAPI、Chroma Bean、全局异常 | Spring AI Chroma 自动装配被显式禁用，由 `ChromaConfig` 自建 |
| 持久层 | `com.ragqa.repository` | Spring Data JPA | 全部为 `JpaRepository<T, UUID>` |
| 实体 | `com.ragqa.model` | JPA `@Entity` | 5 张表对应实体；主键策略为 `GenerationType.UUID` |
| DTO | `com.ragqa.dto` | API 入参/出参 | 仅 `CreateKnowledgeBaseRequest` 使用 `@Valid` |

### 2.3 核心设计模式与抽象

| 模式 | 体现位置 |
|---|---|
| **Builder** | Spring AI `ChromaApi.builder() / ChromaVectorStore.builder()`、`ChatClient.Builder` |
| **Template Method（变体）** | `TextSplitter.split()` 根据 `chunk.strategy` 配置切换 `fixed/paragraph/recursive` |
| **Strategy（隐式）** | `Bm25SearchService` 与 `ChromaService` 互为可替换的检索策略，由 `HybridSearchService` 通过 RRF 融合 |
| **Registry/Singleton（隐式）** | Spring Bean 单例 + 内存 BM25 倒排索引（`Bm25SearchService.documents` HashMap） |
| **Chain of Responsibility（部分）** | 文档处理：`Tika → OCR 兜底 → Tika → 表格追加` |
| **Specification** | `GlobalExceptionHandler` 统一把 `IllegalArgumentException → 400`，`RuntimeException → 500` |

### 2.4 异步与事务

- **`@EnableAsync`** 在启动类启用（`RagQaApplication.java:26`）
- **`@Async` + `@Transactional(propagation = REQUIRES_NEW)`**：`DocumentProcessService.processDocumentAsync()`（`DocumentProcessService.java:73-74`）
  - **风险**：未配置 `TaskExecutor`，Spring 默认使用 `SimpleAsyncTaskExecutor`（每次新建线程），生产环境应显式声明 `ThreadPoolTaskExecutor` Bean
- **`@Transactional`**：`DocumentService.uploadDocument()`、`KnowledgeBaseService.create/delete()`、`DocumentService.deleteDocument()`

---

## 三、核心业务模块

### 3.1 业务域划分

| 业务域 | 入口 Controller | 核心 Service | 数据实体 |
|---|---|---|---|
| **认证授权** | `AuthController` | `UserService`、`JwtService` | `User` |
| **知识库管理** | `KnowledgeBaseController` | `KnowledgeBaseService` | `KnowledgeBase` |
| **文档生命周期** | `DocumentController` | `DocumentService`、`DocumentProcessService` | `Document`、`DocumentChunk` |
| **RAG 问答** | `ChatController`、`ChatHistoryController` | `ChatService`、`RagService`、`HybridSearchService`、`Bm25SearchService`、`ChromaService`、`EmbeddingService`、`RerankService`、`TextSplitter`、`OcrService`、`TableExtractorService` | `ChatHistory` |

### 3.2 Top 5 最核心的业务方法

> 这些方法改动影响最大，修改前必须通读整条调用链。

| 排名 | 方法 | 文件:行 | 为什么核心 |
|---|---|---|---|
| 1 | `DocumentProcessService.processDocumentAsync()` | `DocumentProcessService.java:73-248` | 文档进入 RAG 系统的唯一入口；串联 Tika → OCR → TextSplitter → Embedding → Chroma → BM25；决定系统能否产出可检索数据 |
| 2 | `RagService.chat()` | `RagService.java:69-120` | 主链路问答入口；含 Chroma 检索 + 知识库过滤 + LLM 调用 + 余额不足友好提示 |
| 3 | `ChromaService.addDocument()` / `similaritySearch()` | `ChromaService.java:115-140 / 148-195` | **绕过 Spring AI 自带 `ChromaVectorStore`，手写 REST 客户端**；是 v2 API + 自定义 tenant/database 的关键适配点 |
| 4 | `DocumentService.uploadDocument()` | `DocumentService.java:62-122` | 同步落盘 + `saveAndFlush()` 立即可见 + `processDocumentAsync()` 触发；是事务边界与异步触发衔接处 |
| 5 | `HybridSearchService.search()` / `hybridSearch()` | `HybridSearchService.java:204-340` | RRF 融合算法实现，是查询质量的关键（**但目前 `RagService` 未调用它**，见风险章节） |

### 3.3 业务关键流程时序

**文档处理链路**（上传 → 可检索）：
```
Client ─POST /api/knowledge-bases/{kbId}/documents──► DocumentController
        │
        ▼
DocumentService.uploadDocument() [TX]
   ├─ 校验 KB 存在 + 文件类型 + 同名检查
   ├─ Files.copy() 写入 ./uploads/{kbId}/{filename}
   ├─ Document 记录入库 status=UPLOADING progress=10
   └─ saveAndFlush()                       ← 关键：让异步线程能读到
        │
        ▼  @Async + REQUIRES_NEW
DocumentProcessService.processDocumentAsync() [新线程 + 新事务]
   ├─ 状态: PARSING(30%)
   ├─ Tika.parseToString()
   │     ├─ .pdf 且文字层<50字 → OcrService.extractTextFromPdf()
   │     └─ .docx → Tika + TableExtractorService.extractTablesFromWord()
   ├─ 状态: CHUNKING(50%)
   ├─ TextSplitter.split()  (默认 recursive, 500/50)
   ├─ 状态: EMBEDDING(70%)
   └─ for each chunk:
       ├─ EmbeddingService.embed() → Ollama /api/embeddings
       ├─ DocumentChunk 入库 (LONGTEXT embedding 字段)
       ├─ ChromaService.addDocument() → Chroma v2 REST
       └─ Bm25SearchService.addDocument()  (内存倒排索引)
   └─ 状态: COMPLETED(100%) / FAILED
```

**问答链路**：
```
Client ─POST /api/chat──► ChatController
   ├─ 流式 → ChatService.streamChat() → ChatClient.stream().content() → Flux<String>
   └─ 非流式 → ChatService.chat() → RagService.chat()
                 ├─ documentRepository.findByKnowledgeBaseId() — 校验是否有 COMPLETED 文档
                 ├─ ChromaService.similaritySearch()  (TOP_K 默认 3)
                 ├─ filter: documentId ∈ {该 KB 的 COMPLETED 文档}
                 ├─ buildContext() + buildPrompt()  (统一提示词模板)
                 └─ ChatClient.call().content()
                    ├─ 空响应 → "AI 服务余额不足"
                    └─ 异常且含 "insufficient_balance" → 同上友好提示
```

---

## 四、数据层分析

### 4.1 数据库与表结构

**类型**：MySQL 8，字符集 `utf8mb4 / utf8mb4_unicode_ci`，全部 `InnoDB`，主键全部为 `CHAR(36)` UUID。

| # | 表名 | 实体 | 主键 | 关键外键 | 索引 |
|---|---|---|---|---|---|
| 1 | `users` | `User` | UUID | — | `username`(UNIQUE) |
| 2 | `knowledge_base` | `KnowledgeBase` | UUID | — | `name`(UNIQUE) |
| 3 | `document` | `Document` | UUID | `knowledge_base_id` | `knowledge_base_id`, `status` |
| 4 | `document_chunk` | `DocumentChunk` | UUID | `document_id` | `document_id`, `chunk_index` |
| 5 | `chat_history` | `ChatHistory` | UUID | `knowledge_base_id`(SET NULL) | `session_id`, `knowledge_base_id`, `created_at` |

**Flyway 迁移**：仅 `V1__init_schema.sql`（`db/migration/V1__init_schema.sql`）。schema 变更请新建 `V2__xxx.sql`，不要改 `V1`。

### 4.2 ORM 框架使用方式

- **Spring Data JPA + Hibernate**，`application.properties:8` 设置 `ddl-auto=validate`（**关键**：实体与 DB 不一致时启动会失败，这是好事）
- 实体使用 Lombok `@Data`，主键 `GenerationType.UUID`（Hibernate 自动生成）
- 全部 Repository 仅靠方法名派生查询（`findByUsername`、`findByKnowledgeBaseId`、`deleteBySessionId` 等），**无 `@Query` / 无自定义 JPQL**
- **`document_chunk.embedding` 字段为 `LONGTEXT`**，存的是 `Float[]` 的 `Arrays.toString()` 字符串（如 `"[0.1, 0.2, ...]"`），由 `RagService.parseEmbedding()` 反序列化（`RagService.java:274-291`）

### 4.3 事务与并发

- **事务粒度**：仅 `DocumentService.uploadDocument`、`DocumentService.deleteDocument`、`KnowledgeBaseService.create/delete` 标注 `@Transactional`
- **`DocumentProcessService.processDocumentAsync` 使用 `REQUIRES_NEW`**，确保在新线程中独立事务，与上传主事务解耦
- **没有分库分表**，单库单实例
- **`spring.jpa.show-sql=false`**（`application.properties:9`）—— 调试 SQL 时改为 `true`

### 4.4 关键 Repository 方法速查

| Repository | 方法 | 用途 |
|---|---|---|
| `UserRepository` | `findByUsername` / `existsByUsername` / `existsByEmail` | 登录与注册查重 |
| `KnowledgeBaseRepository` | `findByName` / `existsByName` | 创建查重 |
| `DocumentRepository` | `findByKnowledgeBaseId` / `findByKnowledgeBaseIdAndFileName` | 列表查询 + 上传重名检查 |
| `DocumentChunkRepository` | `findByDocumentId` / `deleteByDocumentId` | 删除文档时级联清理 |
| `ChatHistoryRepository` | `findBySessionIdOrderByCreatedAtAsc` / `findByKnowledgeBaseIdOrderByCreatedAtDesc` / `deleteBySessionId` | 会话历史 |

---

## 五、接口与交互

### 5.1 REST 接口清单（共 14 个）

| Method | Path | 控制器 | 鉴权 | 备注 |
|---|---|---|---|---|
| POST | `/api/auth/register` | `AuthController` | 公开 | 返回 `{token, username}` |
| POST | `/api/auth/login` | `AuthController` | 公开 | 同上 |
| GET | `/api/knowledge-bases` | `KnowledgeBaseController` | JWT | 列表 |
| POST | `/api/knowledge-bases` | `KnowledgeBaseController` | JWT | 名称不可重复 |
| GET | `/api/knowledge-bases/{id}` | `KnowledgeBaseController` | JWT | |
| DELETE | `/api/knowledge-bases/{id}` | `KnowledgeBaseController` | JWT | **删除知识库不会级联删除 Chroma 向量**（见风险） |
| POST | `/api/knowledge-bases/{kbId}/documents` | `DocumentController` | JWT | `multipart/form-data`，字段名 `file` |
| GET | `/api/knowledge-bases/{kbId}/documents` | `DocumentController` | JWT | |
| GET | `/api/documents/{id}` | `DocumentController` | JWT | |
| DELETE | `/api/documents/{id}` | `DocumentController` | JWT | **正确**级联：Chroma + Chunks + 文件 + Document |
| POST | `/api/chat` | `ChatController` | JWT | 普通问答，返回 `String` |
| POST | `/api/chat/stream` | `ChatController` | JWT | SSE `Flux<String>`，`MediaType.TEXT_EVENT_STREAM_VALUE` |
| GET | `/api/chat-history`、`/api/chat-history/{sessionId}`、`/api/knowledge-bases/{kbId}/chat-history` | `ChatHistoryController` | JWT | |
| POST | `/api/chat-history` | `ChatHistoryController` | JWT | |
| DELETE | `/api/chat-history/{sessionId}` | `ChatHistoryController` | JWT | |

### 5.2 统一返回格式

**没有全局 `ResponseBodyAdvice`/统一包装**，控制器直接返回实体或 DTO：

- 成功：`ResponseEntity.ok(body)` 或 `ResponseEntity.status(HttpStatus.CREATED)`
- 删除：`ResponseEntity.noContent().build()`（204）
- 业务异常（`IllegalArgumentException`）：由 `GlobalExceptionHandler` 包装为 `400 { "error": "..." }`
- 运行时异常：包装为 `500 { "error": "..." }`

### 5.3 鉴权

- **`SecurityConfig.securityFilterChain()`**（`SecurityConfig.java:106-136`）声明：
  - `/api/auth/**` 公开
  - `/swagger-ui/**`、`/v3/api-docs/**`、`/actuator/health` 公开
  - `/api/**` 需要 JWT
  - 其余放行
- **会话策略**：`SessionCreationPolicy.STATELESS`
- **密码**：`BCryptPasswordEncoder`
- **JWT**：`JwtService`（`service/JwtService.java`），HS256，密钥 base64 编码（`jwt.secret`），默认 24h 过期
- **当前未做角色/权限分级**，所有登录用户都是 `ROLE_USER`

### 5.4 接口文档

`OpenApiConfig`（`config/OpenApiConfig.java`）声明 `bearerAuth` 安全方案，所有接口默认要求 Bearer Token。访问：
- Swagger UI：`http://localhost:8080/swagger-ui.html`
- OpenAPI JSON：`/v3/api-docs`

### 5.5 外部系统对接

| 系统 | 用途 | 接入点 | 默认地址 |
|---|---|---|---|
| **LLM（OpenAI 协议）** | 问答生成 | `application.properties:23-25` + `spring-ai-starter-model-minimax` | `${OPENAI_BASE_URL}`，模型 `MiniMax-M2.5` |
| **Ollama（本地）** | Embedding 生成 | `EmbeddingService` 直接 HTTP | `http://localhost:11434`，模型 `qwen3-embedding:4b` |
| **Chroma（向量库）** | 向量存储与检索 | `ChromaService` 自实现 REST | `http://localhost:8000`，tenant=`SpringAiTenant`，database=`SpringAiDatabase` |
| **Tesseract OCR** | 扫描 PDF 文字识别 | `OcrService` JNI 调用 | 本机需 `brew install tesseract tesseract-lang` |
| **MySQL** | 元数据持久化 | Spring Data JPA | `localhost:3306`，DB `ragqa` |

---

## 六、配置与运维

### 6.1 多环境配置

**`application.properties` 是唯一主配置**（无 `application-dev.yml` 等 profile 文件），差异通过 `.env` 中的环境变量切换。`.env.example` 列出了所有可调项。

**测试环境**通过 `src/test/resources/application-test.yml` 提供，覆盖：H2 内存库（`MySQL 兼容模式`）、`ddl-auto=create-drop`、关闭 Flyway、关闭流式。

### 6.2 启动方式

**本地开发（IDE/Maven）**：
```bash
cd rag-qa-backend
cp ../.env.example ../.env  # 编辑填入真实 API_KEY
./mvnw spring-boot:run
# 或 mvn spring-boot:run
```

**Docker Compose（推荐完整链路）**：
```bash
# 项目根目录
cp .env.example .env  # 编辑
docker-compose up -d   # 启动 mysql + chroma + backend + frontend
```

### 6.3 关键启动参数

`application.properties` 中已配置（均可由 `.env` 覆盖）：

| 配置 | 默认值 | 说明 |
|---|---|---|
| `DB_HOST/PORT/NAME/USER/PASSWORD` | localhost/3306/ragqa/root/(空) | MySQL 连接 |
| `OPENAI_API_KEY` | **必填** | LLM API 密钥 |
| `OPENAI_BASE_URL` | `https://api.minimax.chat` | 兼容 OpenAI 协议 |
| `OPENAI_MODEL` | `MiniMax-M2.5` | 模型名 |
| `OLLAMA_BASE_URL` | `http://localhost:11434` | 本地 Embedding 服务 |
| `OLLAMA_EMBEDDING_MODEL` | `qwen3-embedding:4b` | Embedding 模型 |
| `CHROMA_URL` | `http://localhost:8000` | Chroma 服务地址 |
| `FILE_UPLOAD_DIR` | `./uploads` | 文件落地目录 |
| `chunk.strategy` | `recursive` | `fixed`/`paragraph`/`recursive` |
| `chunk.size` | `500` | 切片字符数 |
| `chunk.overlap` | `50` | 切片重叠 |
| `ocr.enabled` | `true` | 是否启用 OCR |
| `ocr.language` | `chi_sim+eng` | Tesseract 语言包 |
| `hybrid.retrieval.enabled` | `true` | 是否启用混合检索 |
| `hybrid.retrieval.vector_weight` | `0.7` | 向量检索权重 |
| `hybrid.retrieval.bm25_weight` | `0.3` | BM25 权重 |
| `hybrid.retrieval.topk` | `3` | 最终返回数 |
| `rerank.enabled` | `false` | Cross-Encoder 重排（**简化版**，关键词打分） |
| `chat.streaming` | `true` | 是否流式 |
| `retrieval.topk` | `3` | 向量检索 TopK |
| `jwt.expiration` | `86400000` (24h) | JWT 过期 |
| `spring.servlet.multipart.max-file-size` | `50MB` | 上传限制 |

### 6.4 日志

**没有 logback-spring.xml**，全部走默认配置 + `logging.pattern.console`。`logging.level.com.ragqa=DEBUG` 已开（生产应改为 `INFO`）。DEBUG 级别涉及：`org.springframework.ai`、`org.springframework.web`、`org.springframework.http`、`org.springframework.security`，输出会很冗长。

### 6.5 定时任务 / 异步 / 消息

- **定时任务**：**无 `@Scheduled` / 无 Quartz**。文档处理通过 `@Async` 触发，无独立调度器
- **异步执行器**：**未显式配置**，使用 Spring Boot 默认 `SimpleAsyncTaskExecutor`（**生产隐患**，见下文）
- **消息队列**：**无 RabbitMQ/Kafka**
- **缓存**：**无 Redis/Caffeine**

### 6.6 监控

- Spring Boot Actuator 仅暴露 `/actuator/health`、`/actuator/info`（`management.endpoints.web.exposure.include=health,info`）
- Docker 健康检查：`curl -f http://localhost:8080/actuator/health`

---

## 七、风险与上手建议

### 7.1 技术债务与潜在坑点（按严重度排序）

| 严重度 | 问题 | 位置 | 影响 |
|---|---|---|---|
| 🔴 **高** | **`@Async` 未配置线程池**，使用默认 `SimpleAsyncTaskExecutor`，每次新建线程 | `RagQaApplication.java:26` + `DocumentProcessService.java:73` | 高并发上传会耗尽线程；无队列、无拒绝策略 |
| 🔴 **高** | **`RagService.retrieve()` 未走混合检索**，`HybridSearchService` 已被实现但未被主链路调用 | `RagService.java:131-160` vs `HybridSearchService.java` | `hybrid.retrieval.*` 配置项形同虚设；用户预期与实际行为不符 |
| 🔴 **高** | **JWT 密钥硬编码且未 base64 编码**（`mySecretKeyForJWTTokenGenerationThatIsLongEnough123456`），但 `JwtService.getSignInKey()` 调用 `Decoders.BASE64.decode()`，密钥会被原样做 base64 解码（实际是 ASCII 通过 base64 编码后当密钥） | `application.properties:90` + `JwtService.java:74-77` | 看似有 secret 但实际效果是固定的"假安全"；生产环境必须使用环境变量注入真实密钥 |
| 🔴 **高** | **删除知识库不级联清理 Chroma 向量** | `KnowledgeBaseService.delete()` (`KnowledgeBaseService.java:74-79`) | 留下孤儿向量，污染后续检索；`document_chunk` 会通过 `ON DELETE CASCADE` 清理，但 Chroma 中的向量永久留存 |
| 🟡 **中** | **`DocumentController` 删除文档时没有事务包裹 Chroma 调用** | `DocumentService.deleteDocument()` (line 148-167) | Chroma 删除失败时已有 try-catch 兜底（log.warn），但 MySQL 部分删除会留下不一致状态 |
| 🟡 **中** | **`ChatHistoryController` 直接使用 Repository，绕过 Service** | `ChatHistoryController.java` 整文件 | 未来加缓存/审计/消息通知时必须改 Controller，破坏分层 |
| 🟡 **中** | **`EmbeddingService` 使用 `RestTemplate` 而非 `WebClient`**，且手动拼接 JSON（含字符串转义） | `EmbeddingService.java:65-122` | JSON 拼接脆弱（特殊字符若处理不全会破坏请求）；阻塞线程 |
| 🟡 **中** | **`GlobalExceptionHandler` 异常日志缺失**，只返回 message | `GlobalExceptionHandler.java` 整文件 | 生产排障困难，应至少 `log.warn` |
| 🟡 **中** | **`RerankService.scoreCandidates` 实际是关键词打分**，注释中提到 Cross-Encoder 模型但未真正调用 | `RerankService.java:260-313` | 启用 `rerank.enabled=true` 后提升有限，且与"Cross-Encoder 重排"的命名承诺不符 |
| 🟡 **中** | **`Bm25SearchService` 是内存索引**，进程重启会清空 | `Bm25SearchService.java:171-193` | 启动后第一次检索会缺少 BM25 评分，自动降级为纯向量检索（`HybridSearchService.vectorOnlySearch`） |
| 🟡 **中** | **`Bm25SearchService.tokenize` 中文按字符切分** | `Bm25SearchService.java:474-502` | 注释中已承认问题；生产环境中文检索应换 jieba/HanLP/IK |
| 🟢 **低** | **`GlobalExceptionHandler` 没有 `@ExceptionHandler(MethodArgumentNotValidException.class)`** | `GlobalExceptionHandler.java` | `@Valid` 校验失败时返回 500 而非 400 |
| 🟢 **低** | **`KnowledgeBaseResponse.from` 不在 DTO 包内的工厂方法** | `KnowledgeBaseResponse.java:45` | import 了 `com.ragqa.model.KnowledgeBase`，造成 `dto → model` 反向依赖 |
| 🟢 **低** | **`Spring Boot 3.3` 与 `Spring AI 1.1.3` 兼容性靠 BOM 保证**，未来 Spring AI 主版本升级可能引发连锁问题 | `pom.xml:22-35` | 升级前需对照 BOM 清单 |
| 🟢 **低** | **`DocumentRepository` 缺少分页**，列表接口返回全量数据 | `DocumentRepository.java:11-15` + `DocumentService.getDocumentsByKnowledgeBase` | 文档量大时内存爆炸 |
| 🟢 **低** | **`OcrService.initTesseract` 写死了 macOS 路径**（`/opt/homebrew/Cellar/tesseract/5.5.2/...`），Linux/Windows 不适配 | `OcrService.java:172-179` | 部署到 Linux 时必须设 `TESSDATA_PREFIX` 环境变量 |
| 🟢 **低** | **`spring.main.allow-circular-references=true`** | `application.properties:94` | 反映代码中存在循环依赖；应重构而非开启此开关 |

### 7.2 新手快速上手路线

**第一步（30 分钟）—— 通读骨架**：
1. `rag-qa-backend/src/main/java/com/ragqa/RagQaApplication.java` — 启动入口
2. `rag-qa-backend/src/main/resources/application.properties` — 配置全集
3. `pom.xml` — 依赖清单与版本
4. `rag-qa-backend/src/main/resources/db/migration/V1__init_schema.sql` — 一张图看 5 张表
5. `docs/plans/*-srs.md`、`docs/plans/*-design.md`、`docs/test-cases/*.md`、`feature-list.json` — 业务全貌（feature-list.json 16 个特性全部 passing）

**第二步（30 分钟）—— 看懂主链路**：
1. `controller/DocumentController.java` → `service/DocumentService.java` → `service/DocumentProcessService.java` （上传链路）
2. `controller/ChatController.java` → `service/ChatService.java` → `service/RagService.java` （问答链路）
3. 重点看 `DocumentProcessService.processDocumentAsync()` 这一个方法，把 4 个阶段（PARSING/CHUNKING/EMBEDDING/COMPLETED）的状态机搞清楚

**第三步（30 分钟）—— 看懂认证**：
1. `config/SecurityConfig.java` + `config/JwtAuthenticationFilter.java` + `service/JwtService.java` + `service/UserService.java`
2. 重点理解 `OncePerRequestFilter` + `@Lazy` 解决循环依赖的写法

**第四步（实操）—— 本地启动**：
```bash
# 1. 准备依赖
brew install mysql chroma tesseract tesseract-lang
brew services start mysql
docker run -d -p 8000:8000 --name chroma chromadb/chroma
ollama pull qwen3-embedding:4b && ollama serve  # 后台运行

# 2. 启动后端
cd rag-qa-backend
cp ../.env.example ../.env && vi ../.env   # 填 OPENAI_API_KEY
./mvnw spring-boot:run

# 3. 启动前端
cd ../rag-qa-frontend
npm install && npm run dev

# 4. 浏览器访问
open http://localhost:5173   # 前端
open http://localhost:8080/swagger-ui.html  # API 文档
```

**调试技巧**：
- `application.properties:8` 改 `spring.jpa.show-sql=true` 看 SQL
- `application.properties:97` 把 `com.ragqa` 调到 `TRACE` 看完整链路
- 文档上传失败查 `document.error_message` 字段（`Document` 实体）
- OCR 失败先检查 `TESSDATA_PREFIX` 环境变量与 `OcrService.getConfigInfo()` 输出
- Chroma 失败用 `curl http://localhost:8000/api/v1/heartbeat` 验证连通性

### 7.3 二次开发注意事项

1. **改业务前先看 `feature-list.json` 与 `RELEASE_NOTES.md`**，16 个特性已标记完成，避免重复造轮子
2. **新增实体字段必须同步**：① Entity 类 ② Flyway 新增 `V2__xxx.sql`（不要改 V1）③ JPA `ddl-auto=validate` 会卡启动来兜底
3. **新增接口若需要鉴权**：在 `SecurityConfig.securityFilterChain()` 中放行白名单，否则默认要求 JWT
4. **新增服务若涉及切分/Embedding**：优先复用 `TextSplitter` 与 `EmbeddingService`，不要重复实现
5. **任何 Chroma 改动都要同时考虑 v2 API 的 tenant/database 字段**：`tenantName=SpringAiTenant`、`databaseName=SpringAiDatabase` 已在多处硬编码
6. **新增 Controller 严禁直接注入 Repository**（参考 `ChatHistoryController` 的反面教材），保持 Controller → Service → Repository 分层
7. **流式接口沿用 `Flux<String>` + `MediaType.TEXT_EVENT_STREAM_VALUE`**，注意 Spring WebFlux 与 Spring MVC 共存（本项目仍以 MVC 为主，`Flux` 仅用于流式返回）

---

## 八、最优先的 3 条上手行动建议

> 如果只能做 3 件事，强烈建议先做这些：

1. **【必做】建立本地完整运行环境并跑通"创建知识库 → 上传 PDF → 流式问答"主链路**（按 7.2 第四步）。RAG 项目依赖外部服务多（MySQL + Chroma + Ollama + Tesseract + LLM API），任何一环不通都看不到效果，先打通链路再读代码事半功倍。

2. **【强烈推荐】通读 `DocumentProcessService.processDocumentAsync()` 这一方法，并对照 `RagService.chat()` 画出"上传 → 检索"完整时序图**（10 分钟即可）。这两个方法是系统最核心的入口，覆盖了文档生命周期 + 检索融合 + LLM 调用三大主题；理解了它们，再看其他 Service 都是工具类组合。

3. **【关键风险排查】第一周就处理"`@Async` 无线程池"与"删除知识库不清理 Chroma"两个 🔴 高危问题**（`DocumentProcessService.java:73` 与 `KnowledgeBaseService.java:74-79`）。前者加一个 `ThreadPoolTaskExecutor` Bean（推荐 core=4, max=8, queue=100），后者在 `KnowledgeBaseService.delete()` 中先查所有 `documentId` 再批量调 `chromaService.deleteByDocumentId`。这两个坑不修，上生产必爆。

---

> 文档生成于 2026-06-27，基于对 `rag-qa-backend` 全部 33 个生产 Java 文件 + `pom.xml` + `application.properties` + `V1__init_schema.sql` + `docker-compose.yml` + `Dockerfile` + `.env.example` + 7 个测试文件的完整阅读。如需对某个模块做更深入的剖析（例如 Chroma v2 API 协议细节、RRF 算法数学推导、Flyway 迁移策略），可在此文档基础上展开。
